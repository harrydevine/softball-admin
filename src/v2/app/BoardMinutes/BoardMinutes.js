import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const BoardMinutes: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      Board Minutes Page Title
    </Title>
  </PageSection>
);

export { BoardMinutes };
