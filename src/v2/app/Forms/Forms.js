import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Forms: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      Forms & Documents Page Title
    </Title>
  </PageSection>
);

export { Forms };
