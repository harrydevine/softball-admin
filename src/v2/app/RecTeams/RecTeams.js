import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const RecTeams: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      Rec Teams Page Title
    </Title>
  </PageSection>
);

export { RecTeams };
